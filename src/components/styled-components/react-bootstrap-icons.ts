import styled from "styled-components";
import { Plus, Search, Bell, List } from "react-bootstrap-icons";

export const AddRecipeButton = styled(Plus)``
export const NavSearchButton = styled(Search)``
export const NotificationBellButton = styled(Bell)``
export const ThreeLineMenu = styled(List)``
